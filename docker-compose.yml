version: '2.1'
services:
  jenkins:
    image: 'amrit96/jenkins:latest'
    ports:
      - '80:8080'
      - '443:8443'
      - '50000:50000'
    volumes:
    - './jenkins_data:/var/jenkins_home'
volumes:
    jenkins_data:
        driver: local
